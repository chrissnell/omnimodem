<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme so the shell needs no AppCompat/Material dependency. -->
    <style name="Theme.Omnimodem" parent="@android:style/Theme.Material.Light.NoActionBar"/>
</resources>
